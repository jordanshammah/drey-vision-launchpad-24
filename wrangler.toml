# Cloudflare Wrangler config for deploying to Pages/Workers.
# Replace the placeholder values with your Cloudflare account info.

name = "drey-vision-launchpad-24"
# Your Cloudflare Account ID (set via `wrangler whoami` or from dashboard)
account_id = "7f2cdb78924b5e7edcf082c4b9b1d1b6"

# Compatibility date (required by Wrangler)
compatibility_date = "2026-01-01"

# Required by Pages when using wrangler.toml
pages_build_output_dir = "./dist"
